{{#if dashboardPosts}}
{{#each dashboardPosts}}
{{#if post}}
<div class="dashboard">
    <ul class="container">
        <li>
            <a href="/edit-post">
                <span>{{title}}</span>
                <span>{{date}}</span>
            </a>
        </li>
    </ul>
</div>
{{/if}}
{{/each}}
{{else}}
<form class="new-post" action="/new-post">
    <button type="submit" class="new-post-btn">+ New Post</button>
</form>
{{/if}}