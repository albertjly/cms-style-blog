{{#if dashboardPosts}}
{{#each dashboardPosts}}
<div class="dashboard">
    <ul class="container">
        <li>
            <a href="/edit-post">
                {{> dashboard-post}}
            </a>
        </li>
    </ul>
</div>
{{/each}}
{{/if}}

<form class="new-post" action="/new-post">
    <button type="submit" class="new-post-btn">+ New Post</button>
</form>